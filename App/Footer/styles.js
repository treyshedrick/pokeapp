import {StyleSheet} from 'react-native';

module.exports = StyleSheet.create({
  footerContainer: {
    height: '10%',
    backgroundColor: 'red',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
