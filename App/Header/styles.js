import {StyleSheet} from 'react-native';

module.exports = StyleSheet.create({
  headerContainer: {
    height: '10%',
    backgroundColor: 'red',
    alignItems: 'center',
    justifyContent: 'center',
  },

  sectionTitle: {
    marginTop: 30,
  },
});
