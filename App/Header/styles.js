import {StyleSheet} from 'react-native';

module.exports = StyleSheet.create({
  headerContainer: {
    height: '10%',
    backgroundColor: 'red',
  },

  sectionTitle: {
    textAlign: 'center',
    paddingTop: 40,
    fontSize: 24,
    fontWeight: '600',
    color: 'white',
  },
});
